name: 🚀 CI/CD Gestion Event Platform

on:
  push:
    branches: [ 'main' ]
  pull_request:
    branches: [ 'main' ]
  workflow_dispatch:

jobs:
  ## 🧪 STAGE 1: TESTS & BUILD ✅ FIXÉ
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'
          
      - name: 🧪 Maven Build **COMPLÈTE**
        run: mvn clean install -DskipTests  # ✅ TOUS modules + target/classes
      
      - name: 📦 Upload JARs + CLASSES
        uses: actions/upload-artifact@v4
        with:
          name: maven-artifacts
          path: |
            **/target/*.jar
            **/target/classes
            **/target/test-classes

  ## 🔍 STAGE 2: SONARCLOUD ✅ FIXÉ
  sonar:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'
          
      - name: 📦 Download COMPLETE artifacts
        uses: actions/download-artifact@v4
        with:
          name: maven-artifacts
      
      - name: 🔍 SonarCloud Analysis **PARFAIT**
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          mvn sonar:sonar \
            -Dsonar.projectKey=meryamradouani_gestion-event-platform \
            -Dsonar.organization=meryamradouani \
            -Dsonar.host.url=https://sonarcloud.io \
            -Dsonar.qualitygate.wait=false

  ## 🐳 STAGE 3: DOCKER ✅ INCHANGÉ
  docker:
    needs: [test, sonar]
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [api-gateway, auth-service, event-service, notification-service, profil-service, registration-service]
    steps:
      - uses: actions/checkout@v4
      
      - name: 📦 Download JARs
        uses: actions/download-artifact@v4
        with:
          name: maven-artifacts  # ✅ Nom changé
      
      - name: 🤖 Docker Login
        uses: docker/login-action@v3
        with:
          username: meryamm
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          
      - name: 🐳 Build & Push
        run: |
          docker build -t meryamm/gestion_event-${{ matrix.service }}:latest \
            -f ${{ matrix.service }}/Dockerfile ./${{ matrix.service }}/
          docker push meryamm/gestion_event-${{ matrix.service }}:latest
